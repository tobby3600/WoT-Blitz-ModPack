[auto][a] property float2 viewportOffset;
[auto][a] property float2 viewportSize;

__forceinline float getLodTransition(float2 projPos, float threshold)
{
	#include "lod-gen-random.slh"

	return lerp(float(threshold + 1.0 <= lodParam.w), float(threshold > lodParam.w), step(0.0, threshold));
}
